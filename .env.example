# ─────────────────────────────────────────────────────────────────────────────
# ASAF by NouchiX — Environment Variables
# Copy this file to .env.local for local dev. Never commit real secrets.
# ─────────────────────────────────────────────────────────────────────────────

# ── Supabase (Auth + DB) ──────────────────────────────────────────────────────
# Get these from: https://supabase.com/dashboard → your project → Settings → API
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=your-anon-public-key
VITE_SUPABASE_PROJECT_ID=your-project-id

# ── ASAF Backend API (server-side proxy — NOT exposed to browser) ─────────────
# The Next.js app proxies scan requests server-side via /api/scan/* routes.
# This URL is server-side only — no NEXT_PUBLIC_ prefix — no CORS needed.
# VPS deploy: http://localhost:45444 (asaf-api systemd service, same host)
# Local dev:  http://localhost:45444 (run: make run-agent)
ASAF_API_URL=http://localhost:45444

# Optional API key if ASAF_API_KEY is set on the API server
ASAF_API_KEY=

# Allow free-tier onboarding scans without a paid license (required for free scan funnel)
ASAF_ALLOW_EVAL_WITHOUT_LICENSE=true

# ── Stripe ────────────────────────────────────────────────────────────────────
# Server-side only (never expose to frontend)
# Get from: https://dashboard.stripe.com/apikeys
STRIPE_SECRET_KEY=sk_test_...

# The price ID for the "Certify" plan ($99 one-time payment)
# Create at: https://dashboard.stripe.com/products → Add product → Add price → one-time $99
# Copy the price ID (starts with price_...)
STRIPE_PRICE_ID=price_...

# ── App URL ───────────────────────────────────────────────────────────────────
# Your production domain — used for Stripe redirect URLs
# Must match what's configured in Vercel and Stripe dashboard
NEXT_PUBLIC_APP_URL=https://adinkhepra.com

# ─────────────────────────────────────────────────────────────────────────────
# Vercel: add all NEXT_PUBLIC_* vars in Project Settings → Environment Variables
# Fly.io: add server-side vars with: fly secrets set KEY=value
# Stripe webhooks: configure at dashboard.stripe.com/webhooks pointing to
#   https://adinkhepra.com/api/checkout/webhook (future — for subscription events)
# ─────────────────────────────────────────────────────────────────────────────
