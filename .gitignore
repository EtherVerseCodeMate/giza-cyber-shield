bin/
dist/
# vendor/ - REMOVED: We need vendor/ for air-gapped builds
*.exe
*.log
.vscode/

# Private Documentation
#docs/INTEGRATION_ROADMAP.md
#docs/INTEL_REPORT_2025_12.md
#docs/SURVIVAL_STRATEGY.md

# Logs
logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist-ssr
*.local

# Editor directories and files
!.vscode/extensions.json

.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.next/

# Security Artifacts — NEVER commit keys to this repo
keys/
test_fire/

# All test-generated key files (generated by adinkhepra.py _test_pqc_key_gen)
# Root cause of 2026-05-10 key exposure incident — purged from history
test_key_*

# All VPS key material (hostinger_vps_*, vps_key_*)
# These were committed and purged — any regenerated version must NEVER be re-committed
hostinger_vps_*
vps_key_*

# SSH key material — catch-all for any private/public SSH keys at repo root
*_ed25519
*_ed25519.pub
*_ecdsa
*_ecdsa.pub
*_rsa
*_rsa.pub
*_dsa
*_dsa.pub

# PQC key material (Dilithium, Kyber)
*_dilithium
*_dilithium.pub
*_dilithium.pub.adinkhepra.json
*_dilithium.pub.khepra.json
*_kyber
*_kyber.pub

# Generic private key formats
*.pem
id_*
authorized_keys

# Internal
docs/internal/


# Large Assets
pkg/llm/ollama/blobs/
pkg/llm/*/blobs/
pkg/llm/*/blobs/
*.kpkg
*.tar.gz

# Stripped from history (2026-05-11) — too large for GitHub
docs/cyber-brain/
docs/top-secret/
data/vulnerabilities.db
*.exe~
tools/python4pentesters/Downloads/

# CVE Database (fetched on demand via `make fetch-cve` or build)
# Run: make fetch-cve OR data/cve-database/fetch-cve-data.sh
data/cve-database/cve-data/



# Python
__pycache__/
*.pyc

# Temporary / Garbage
NUL
.claude/
demo-snapshot.json
*.tmp
ANONYMOUS_GIT_*.md
pkg/llm/ollama/manifests/registry.ollama.ai/library/gemma3/4b
.vercel
.env*.local

# ── Trestle / OSCAL compliance workspace ──────────────────────────────────────
# Public NIST catalogs — re-downloadable, too large to commit (10MB+)
catalogs/
# Trestle dist output — regenerated from source
dist/
# Downloaded/temporary artifacts
mapping-collections/
# Local env overrides for govcloud validator (contains real resource IDs)
secred-govcloud.env.local.sh
*.env.local.sh
# KEEP in git: profiles/, ASAF-GovCloud-SSP/, system-security-plans/,
#              assessment-plans/, assessment-results/, plan-of-action-and-milestones/,
#              component-definitions/ — these are the compliance evidence artifacts
adinkhepra-asaf-ironbank
