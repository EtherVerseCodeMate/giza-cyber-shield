---
x-trestle-add-props: []
  # Add or modify control properties here
  # Properties may be at the control or part level
  # Add control level properties like this:
  #   - name: ac1_new_prop
  #     value: new property value
  #
  # Add properties to a statement part like this, where "b." is the label of the target statement part
  #   - name: ac1_new_prop
  #     value: new property value
  #     smt-part: b.
  #
x-trestle-set-params:
  # You may set values for parameters in the assembled SSP by adding
  #
  # ssp-values:
  #   - value 1
  #   - value 2
  #
  # below a section of values:
  # The values list refers to the values in the resolved profile catalog, and the ssp-values represent new values
  # to be placed in SetParameters of the SSP.
  #
  A.03.13.11.ODP.01:
    profile-values:
      - <REPLACE_ME>
    profile-param-value-origin: <REPLACE_ME>
x-trestle-global:
  profile:
    title: ASAF CMMC Level 2 Profile — AdinKhepra Protocol (SecRed Knowledge 
      Inc.)
    href: trestle://profiles/ASAF-CMMC-L2/profile.json
  sort-id: 03.13.11
---

# SP_800_171_03.13.11 - \[System and Communications Protection\] Cryptographic Protection

## Control Statement

## Control Assessment Objective

the following types of cryptography are implemented to protect the confidentiality of CUI: [types of cryptography].

## Control guidance

Cryptography is implemented in accordance with applicable laws, Executive Orders, directives, regulations, policies, standards, and guidelines. FIPS-validated cryptography is recommended for the protection of CUI.

______________________________________________________________________

## What is the solution and how is it implemented?

<!-- For implementation status enter one of: implemented, partial, planned, alternative, not-applicable -->

<!-- Note that the list of rules under ### Rules: is read-only and changes will not be captured after assembly to JSON -->

### This System

**GovCloud ECS (ASAF API) â€” CUI Asset â€” implemented**

Cryptographic mechanisms are used to prevent unauthorized disclosure of CUI at rest per ODP A.03.13.11.ODP.01.

- **Mechanisms (ODP.01)**: AES-256-GCM via AWS KMS CMK $(System.Collections.Hashtable.KMSKey) for S3/Aurora/DynamoDB. AES-256-GCM in-process for ASAF DAG nodes. Annual KMS key rotation enabled.
- **S3**: All buckets in GovCloud account use SSE-KMS with $(System.Collections.Hashtable.KMSKey). Bucket policy denies s3:PutObject without SSE-KMS. $(System.Collections.Hashtable.LockObj).
- **Aurora**: Storage-level encryption with $(System.Collections.Hashtable.KMSKey). Data encrypted in storage, in transit between Aurora replicas (TLS), and in backup snapshots.
- **DynamoDB**: Tables encrypted at rest with AWS-managed KMS key (AES-256).
- **Secrets Manager**: All secrets encrypted with $(System.Collections.Hashtable.KMSKey). Rotation configured per secret type.
- **ASAF DAG**: In-memory CUI data encrypted with AES-256-GCM before persistence. Encryption keys derived from ML-DSA-65 key seeds stored in Secrets Manager.
- **CRMA**: BitLocker (AES-256 XTS) full-disk encryption on all developer workstations.

#### Implementation Status: implemented

______________________________________________________________________
