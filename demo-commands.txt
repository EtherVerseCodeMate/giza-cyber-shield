# ===== DEMO COMMANDS (Ctrl+C -> Ctrl+V) =====

# Show help
.\bin\adinkhepra.exe

# Network topology
.\bin\adinkhepra.exe network build --input demo-snapshot.json --output topology.json

# Attack paths
.\bin\adinkhepra.exe network attack-paths --topology topology.json --from web-01

# DAG visualization (already running - just switch to browser)
# http://localhost:8080

# Executive report (already open - just switch to browser tab)
# executive-summary.html

# SBOM generation (optional - only if asked)
.\bin\adinkhepra.exe sbom generate --target . --output sbom.json
